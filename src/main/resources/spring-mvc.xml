<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- 组件扫描，只扫描controller包 -->
    <context:component-scan base-package="org.cloudlibrary.controller" use-default-filters="false">
        <context:include-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
    </context:component-scan>

    <!-- 视图解析器 -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/jsp/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <!-- 文件上传配置 -->
    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <!-- 设置上传文件的最大尺寸为10MB -->
        <property name="maxUploadSize" value="10485760"/>
        <!-- 设置单个文件的最大尺寸为5MB -->
        <property name="maxUploadSizePerFile" value="5242880"/>
    </bean>

    <!-- 静态资源映射 -->
    <mvc:resources mapping="/static/**" location="/static/"/>
    <mvc:resources mapping="/uploads/**" location="/uploads/"/>

    <!-- 认证拦截器 -->
    <bean id="authInterceptor" class="org.cloudlibrary.interceptor.AuthInterceptor"/>
    <mvc:interceptors>
        <!-- 拦截管理员路径，排除登录接口 -->
        <mvc:interceptor>
            <mvc:mapping path="/admin/**"/>
            <mvc:exclude-mapping path="/admin/toLogin"/>
            <mvc:exclude-mapping path="/admin/login"/>
            <ref bean="authInterceptor"/>
        </mvc:interceptor>
        <!-- 拦截用户路径，排除登录接口 -->
        <mvc:interceptor>
            <mvc:mapping path="/user/**"/>
            <mvc:exclude-mapping path="/user/toLogin"/>
            <mvc:exclude-mapping path="/user/login"/>
            <ref bean="authInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>

    <!-- 注解驱动 -->
    <mvc:annotation-driven/>
    <!-- <mvc:default-servlet-handler/> -->
</beans>